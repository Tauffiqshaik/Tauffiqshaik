#include<stdio.h>
int main()
{
	int a[]={20,54,18,95};
	int n=sizeof(a)/sizeof(a[0]);
	int max=a[0];
	for(int i=0;i<n;i++)
	{
		if(a[i]>max)
		max=a[i];
	}
	printf("LARGEST ELEMENT OF AN ARRAY =%d",max);
	return 0;
}
